# Règles de redirection simplifiées

# Règle pour les rapports
[[redirects]]
  from = "/reports/*"
  to = "/reports/:splat"
  status = 200

# Règle par défaut pour l'application principale
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200