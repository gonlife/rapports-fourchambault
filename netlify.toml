# Configuration temporaire pour déboguer
# Toutes les redirections sont commentées pour arrêter la boucle

# [[redirects]]
#  from = "/reports/*"
#  to = "/.netlify/functions/verify-auth"
#  status = 200
#  force = true

# Seule règle active pour le moment
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  directory = "functions"